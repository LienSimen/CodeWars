 
using System;
using System.Numerics; // Required for BitOperations
​
public static class Kata
{
public static bool IsPowerOfTwo(int n)
{
    return n > 0 && ((uint)n & (uint)(-(int)n)) == (uint)n;
}
}