 
using System;
using System.Linq;
​
public class Kata
{   
  public static string NameShuffler(string strzor)
  {
    // var parts = strzor.Split(' ');
    // return string.Join(" ", parts.Reverse());
    return string.Join(' ', strzor.Split(" ").Reverse());
  }
}