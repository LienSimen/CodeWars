 
using System.Linq;
using System.Text.RegularExpressions;
​
public class Kata
{
  public static string Stringy(int size)
  {
    return new string(Enumerable.Range(0, size).Select(i => i % 2 == 0 ? '1' : '0').ToArray());
  }
}